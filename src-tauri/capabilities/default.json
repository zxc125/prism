{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "settings", "player-*", "s-*", "live", "tenants", "tenants-*", "login"],
  "permissions": [
    "core:default",
    "opener:default",
    "observer:default",
    "dialog:default"
  ]
}
